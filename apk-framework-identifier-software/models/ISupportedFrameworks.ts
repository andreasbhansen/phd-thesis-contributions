export default interface ISupportedFrameworks {
    tabris?: boolean,
    nativescript?: boolean,
    fuse?: boolean,
    codename1?: boolean,
    titanium?: boolean,
    xamarin?: boolean,
    flutter?: boolean,
    weex?: boolean,
    qtmobile?: boolean,
    cordova?: boolean,
    capacitor?: boolean,
    ionic?: boolean,
    adobe_air?: boolean,
    phonegap?: boolean,
    react_native?: boolean
}